#include<stdio.h>

int main() {
    int x = 1;
    while (x=0)
    {
        printf("hello");
    }

    return 0;
    
}

// As the value of x is set to 1, there will be no output in the given program.